# Project Seed

> **This file is immutable once the collaboration begins.**
> Edit only before the first round starts.

## Objective

[HUMAN: Describe the project goal here. Be specific about what "done" looks like.]

## Constraints

- [List any hard constraints: language, framework, deployment target, etc.]

## Success Criteria

- [Measurable outcomes that define project completion]

## Out of Scope

- [Explicitly list what this project does NOT include]

## Tooling

> Configure matching values in `config.json → validation` before running `--init`.

- **Language / runtime:** [e.g. Python 3.12, Node 20, Go 1.22]
- **Lint command:** [e.g. `ruff check src/ && mypy src/ --ignore-missing-imports`]
- **Test command:** [e.g. `pytest tests/ -v` or `npm test` or `go test ./...`]
- **Source root:** `src/`
- **Test root:** `tests/`
